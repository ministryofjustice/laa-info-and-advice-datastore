subprojects {
    group = 'uk.gov.justice.laa.ia.datastore'
    version = rootProject.version

    // Deferred until the java plugin creates the `implementation` configuration.
    plugins.withId('java') {
        apply from: "$rootDir/dependency-overrides.gradle"
    }
}
