# Composed from the reusable building blocks under docker/compose/, so other repos
# (e.g. laa-record-controlled-work-api) can include the same files individually.
include:
  - docker/compose/include.api.yml
  - docker/compose/include.postgres.yml
  - docker/compose/include.mock-oauth.yml
