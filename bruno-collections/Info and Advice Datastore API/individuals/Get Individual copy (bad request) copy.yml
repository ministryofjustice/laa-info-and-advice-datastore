info:
  name: Get Individual (bad request)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v0/individuals/:id"
  params:
    - name: id
      value: helloworld
      type: path
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should be 400 response", function () {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 400 Response
    description: Bad request
    response:
      status: 400
      statusText: Bad request
      body:
        type: text
        data: ""
