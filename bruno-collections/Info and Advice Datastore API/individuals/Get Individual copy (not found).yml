info:
  name: Get Individual (not found)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v0/individuals/:id"
  params:
    - name: id
      value: fa4dcafb-d1b5-4365-abb6-d48a57c33d2c
      type: path
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should be 404 response", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 404 Response
    description: Individual not found
    response:
      status: 404
      statusText: Not found
      body:
        type: text
        data: ""
