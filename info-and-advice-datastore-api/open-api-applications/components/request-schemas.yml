components:
  schemas:
    StartApplicationCommand:
      type: object
      required:
        - client
        - applicationType
        - providerOfficeCode
      properties:
        client:
          $ref: '../client-details-components.yml#/components/schemas/CreateClientCommand'
        applicationType:
          type: string
          enum:
            - RCW
            - CLA
        providerOfficeCode:
          type: string

    DeclarationCommand:
      type: object
      required:
        - eTag
        - declarationConfirmation
        - dateSigned
      properties:
        eTag:
          type: integer
          format: int64
          description: ETag value for optimistic concurrency control
        declarationConfirmation:
          type: boolean
        dateSigned:
          type: string
          format: date
          x-extra-annotation: "@jakarta.validation.constraints.PastOrPresent"

    UpdateEvidenceCommand:
      type: object
      required:
        - eTag
      properties:
        eTag:
          type: integer
          format: int64
          description: ETag value for optimistic concurrency control
        evidenceExemptionCode:
          type: string
        evidenceExemptionReason:
          type: string
          maxLength: 400
        incomeEvidenceChecklist:
          type: object
        expenditureCapitalEvidenceChecklist:
          type: object

    UpdateMeansDataCommand:
      type: object
      required:
        - eTag
      properties:
        eTag:
          type: integer
          format: int64
          description: ETag value for optimistic concurrency control
      additionalProperties: true

    UpdateScopingDataCommand:
      type: object
      required:
        - eTag
      properties:
        eTag:
          type: integer
          format: int64
          description: ETag value for optimistic concurrency control
        scopingQuestions:
          type: object
          nullable: true
          additionalProperties: true